# 2026-07-19 工作日志

## UI 商业化升级（专业网文写作工具方向）

### 改动范围
聚焦三个高 ROI 层面，未破坏既有功能与 API 调用：

1. **全局设计 token 增强** — `frontend/src/assets/styles/main.css` 末尾追加专业 SaaS 视觉语言：
   - `.pro-badge` Pro 商业版徽章（亮/暗/黑金三主题兼容）
   - `.cmd-search` 全局命令搜索框（⌘K 风格）
   - `.engine-status` AI 引擎状态徽章（带脉冲动画）
   - `.topbar-icon-btn` 顶栏图标按钮规范
   - `.user-menu-trigger` + `.user-avatar` 用户头像菜单触发器
   - `.sub-nav-bar` + `.sub-nav-item` 二级子导航条
   - `.surface-pro` 增强卡片质感
   - `.hero-banner-pro` 渐变 banner 背景
   - `.metric-trend` 指标趋势药丸（up/down/flat）
   - `.section-eyebrow` 区块小标签
   - `.gradient-text` 渐变文本
   - `.topbar-divider` 顶栏分隔线

2. **AppLayout 顶栏重写** — `frontend/src/layouts/AppLayout.vue`：
   - 左：精致品牌区（衬线 Logo + Pro 徽章 + 衬线品牌名 + mono 字体 descriptor）
   - 中：主导航（图标+文字，活跃态底部蓝色指示条）
   - 中右：全局搜索框（⌘K）+ 引擎状态徽章 + 帮助/通知图标按钮（通知带红点）+ 用户头像下拉菜单
   - 用户菜单接入主题循环切换（light→dark→anchor）、设置打开、路由跳转
   - `/market` 下自动显示二级子导航（爆款发现/趋势大盘/题材研究/爆款拆书）
   - 完整响应式（1280/1080/900/560 断点）

3. **Dashboard 首屏升级** — `frontend/src/views/Dashboard.vue`：
   - Hero 改为 `.hero-banner-pro` 渐变 banner，内嵌装饰光晕 + 网格背景
   - 今日目标卡嵌在 hero 内（毛玻璃卡片），加趋势 +18% 与剩余字数提示
   - 指标卡改为垂直布局（head 含图标+趋势药丸 + info + sparkline 迷你折线图）
   - 数值用 mono 字体 + tabular-nums，标题用 serif 字体
   - 响应式适配新 hero 结构

### 验证
- Vite dev server 启动成功（端口 3001，因 3000 被占）
- `/`、`/src/layouts/AppLayout.vue`、`/src/views/Dashboard.vue` 均返回 HTTP 200，编译无错误
- 后台 dev server task_id: uK9K68

## 下午：基于截图反馈的 UI 细化

1. **Dashboard 指标卡紧凑化** — `frontend/src/views/Dashboard.vue`：
   - 将垂直大卡片改为横向小卡（图标 + 数值/标签 + 趋势药丸）
   - 减小 padding、gap、字体，移除底部 sparkline
   - 药丸字体改用 11px mono，整体更紧凑专业

2. **Dashboard 三列区域排版优化** — `frontend/src/views/Dashboard.vue`：
   - 三列 gap 从 16px 减到 12px，卡片圆角和 padding 更紧凑
   - 标题字号从 15px 减到 14px，图标 15px
   - AI 提醒 item 内边距、图标容器缩小
   - 最近活动时间列宽度调整为 44px 右对齐
   - 作品列表项、封面尺寸、间距全部缩小一档

3. **市场洞察导航改为下拉展开** — `frontend/src/layouts/AppLayout.vue` + `frontend/src/components/market/MarketSectionHeader.vue`：
   - AppLayout 主导航「市场洞察」hover 触发下拉（爆款发现 / 题材研究 / 爆款拆书）
   - 移除 header 下方独立的 sub-nav-bar，避免双重导航
   - MarketSectionHeader 移除内部重复的子导航，只保留返回、标题、描述、操作按钮

4. **修复 Home.vue 顶栏丢失** — `frontend/src/router/index.ts`：
   - 将 `/home` 路由改为使用 `AppLayout` 作为父布局
   - 现在从创作工坊点击「新建作品」进入 /home 后仍保留全局顶栏和导航

5. **Home 创建向导页分类紧凑化** — `frontend/src/components/taxonomy/MarketTaxonomyPicker.vue`：
   - 大类标签：按钮改为 tiny，字体 12px，padding 0 10px，高度 24px，gap 6px
   - 网文主题行：gap 6px，主题 chip 字体 12px / 高度 24px
   - 分类条（市场大类/细分主题/赛道属性/引擎大类）：padding 8px 10px，gap 6px，更紧凑

### 验证
- 所有 6 个修改文件 Vite 编译均返回 HTTP 200
- dev server 仍在后台运行（端口 3001，task_id: uK9K68）

## 下午第二轮：统一风格 + Dashboard 布局重构

1. **统一市场洞察页面风格** — `frontend/src/components/market/MarketSectionHeader.vue`：
   - 移除渐变 banner + serif 大标题的独立风格
   - 改为与 Library/Studio 一致的 `.page-header` + `.page-eyebrow` + `.page-title` + `.page-subtitle` 风格
   - 页面背景用 `radial-gradient(circle at 8% 0%, var(--color-brand-light), transparent 28%)` 与其他页一致
   - 保留返回按钮，放在 page-header 上方一行

2. **Dashboard 顶部布局重构** — `frontend/src/views/Dashboard.vue`：
   - 合并原 hero-section 和 metrics-section 为一个区域
   - hero 改为左右两栏（1.1fr 1fr）：左侧 hero-left（greeting + 今日目标卡垂直排列），右侧 hero-metrics（四个指标卡 2x2 网格）
   - 今日目标卡 padding 从 24x26 减到 18x22，更紧凑
   - 移除不再使用的 NGrid/NGi/NAvatar import
   - 响应式：1100px 时 metrics 变 4 列，768px 时变 2 列

3. **Dashboard 三列区紧凑化** — `frontend/src/views/Dashboard.vue`：
   - dashboard-inner padding 从 32px 减到 24px
   - main-grid gap 从 12px 减到 10px
   - section-card padding 从 16/18 减到 12/16
   - novel-list gap 4→2px，novel-item padding 9→7/8px
   - reminder-list gap 8→6px，reminder-item padding 10→8/10px
   - AI 提醒从 4 条减到 3 条，最近活动从 5 条减到 4 条，最近作品展示 5→4 本
   - 移除不再使用的 AlertCircleOutline/InformationCircleOutline import

4. **模板卡片紧凑化** — `frontend/src/components/market/TemplateCollectionPanel.vue`：
   - template-item min-height 230→150px，padding 20→14px
   - 标题字号 21→19px，h3 字号 17→15px，margin 18/8→10/5px
   - 描述 line-clamp 3→2 行，字号 13→12px
   - template-grid gap 14→10px
   - template-panel padding clamp(20-32)→clamp(16-24)
   - 标签、指标、空状态全部同步紧凑

## 下午第三轮：Dashboard 三栏布局 + 背景全屏修复

1. **Dashboard 顶部改为三栏布局** — `frontend/src/views/Dashboard.vue`：
   - 左侧：保留 AI 创作驾驶舱（greeting 标题、副标题、操作按钮）不变
   - 中间：今日目标卡单独一栏，毛玻璃卡片垂直居中
   - 右侧：四个指标卡片竖排（1 列 × 4 行），等分 hero 高度
   - 修正 `hero-left`/`hero-center`/`hero-right` 三栏结构，补齐缺失的 `hero-main` 结束标签

2. **Dashboard 三列区高度统一、一屏展示** — `frontend/src/views/Dashboard.vue`：
   - `.dashboard-page` 设为 `display: flex; height: 100%`
   - `.dashboard-inner` 使用 flex column 占满剩余空间，hero 固定、三列区 `flex: 1`
   - `.main-grid` 使用 `grid-template-rows: 1fr` + `align-items: stretch`，三列等高
   - `.section-card` 设为 `display: flex; flex-direction: column; height: 100%`
   - 使用 `:deep` 覆盖 n-card 与 n-card__content，使内容区自动撑满
   - 各列表区（novel-list、reminder-list、activity-list）与 generating-item 都 `flex: 1` 居中/顶部对齐
   - 移除 task-card 多余的 `margin-bottom: 10px`，改用 `.main-col` 的 gap 控制

3. **修复页面背景未覆盖全屏** — `frontend/src/layouts/AppLayout.vue` + `frontend/src/views/Library.vue`：
   - 给 `.app-main` 显式设置 `background: var(--app-page-bg)`，避免子页面内容不足时露出空白
   - `.library-page` 增加 `min-height: 100%`，确保内容区铺满 main

### 验证
- Dashboard.vue 编译错误修复后，`/src/views/Dashboard.vue` 返回 HTTP 200
- `/dashboard`、`/market`、`/studio`、`/library` 均返回 HTTP 200
- dev server 仍在后台运行（端口 3001）

